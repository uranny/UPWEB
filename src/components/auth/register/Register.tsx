import React, { useState } from 'react';
import * as S from './style.ts';

function Register() {
    return (
        <S.RegisterContainer>
            Register Page
        </S.RegisterContainer>
    );
}

export default Register;