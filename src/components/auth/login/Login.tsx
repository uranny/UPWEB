import React, { useState } from 'react';
import * as S from './style.ts';

function Login() {
    return (
        <S.LoginContainer>
            Login Page
        </S.LoginContainer>
    );
}

//fun <- kotlin 재밌는 

export default Login;