import React, { useState } from 'react';
import * as S from './style.ts';

function Rally() {
    return (
        <S.RallyContainer>
            Rally Page
        </S.RallyContainer>
    );
}

export default Rally;